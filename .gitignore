# Composer-managed dependencies and tool caches -- regenerated by
# `composer install`, not something CI or other developers need
# committed. A library package doesn't commit its own lock file either
# (consumers resolve their own dependency versions), same reasoning as
# gems/forge_ops_tracker's .gitignore.
/vendor/
/composer.lock
/.phpunit.result.cache
/.php-cs-fixer.cache
